<%- include ../partials/header.html %>
<h3><a class="btn btn-secondary" href="/vehicle_document"><i class="fa fa-plus"></i> Create New</a> Vehicle List</h3>
<table class="table table-striped">
    <thead>
        <tr>
            <th>SL NO</th>
            <th>Vehicle Id</th>
            <th>Image</th>
            <th>Registration Type</th>
            <th>Registration No</th>
            <th>Registration Date</th>         
            <th>Expiry Date</th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% var i=1;
        list.forEach(function(val) { %>
        <tr>
            <td> <%= i %></td>
            <td><%= val.vehicle_id %></td>
            <td><%= val.image %></td> 
            <td><%= val.registration_type %></td>
            <td><%= val.registration_no %></td>
            <td><%= val.registration_date %></td>
            <td><%= val.expiry_date %></td>
            <td>
                <a href="/vehicle_document/<%= val._id %>"><i class="fa fa-pencil fa-lg" aria-hidden="true"></i></a>
                <a href="/vehicle_document/delete/<%= val._id %>" onclick="return confirm('Are you sure to delete this record ?');"><i class="fa fa-trash fa-lg" aria-hidden="true"></i></a>
            </td>
        </tr>
        <% i++; }) %>
    </tbody>
</table>

<%- include ../partials/footer.html %>
